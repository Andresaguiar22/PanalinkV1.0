# Project Knowledge — Panalink

## Branch & Workflow Rules (user-defined)
- Work ONLY on `refactor/full-app-turbo`, directly. Do not branch off; push commits straight there. This was reinstated by the user on 2026-08-20.
- Previous audit branch `audit/reels-comments-fix` was merged; keep only the aforementioned branch alive.

## Zero-Regressions Rules
- Main file is `AGENTS.md` (root). Verify the checklist whenever you touch shared repositories (UploadRepository, SocialSyncWorker, StatesRepository, ViewStateScreen, Reels screens).

## Story Editor (new in e744b92)
- Active editor: `CleanStoryEditorScreen.kt` (photo/video/text + real audio).
- Previous `StoryEditorScreen.kt` remains on disk for Reel; Story entry route `createStory` now points to the new editor.
- Publish path: `StatesRepository.createState(mediaType, caption, mediaFile, audioUrl, isReel=false)` — do NOT use Worker `uploadType="story"` (not supported; only STATE/REEL/PROFILE are).
- Story visibility "only between contacts" is controlled by Supabase RLS (current author+contact relations). `isReel=false` is the flag that makes a post a story (expires 24h).

## Story Audio
- Real audio URL is stored on `UserState.audioUrl` (column `audio_url`).
- `ViewStateScreen` prefers `audioUrl` over the mocked `[Música: ...]` caption catalog; falls back to the catalog when audioUrl is blank.

## Video Cache (reels)
- `CacheDataSourceFactory` uses path+query based stable cache key so the CDN host re-anchor (`CdnManager.resolveMediaUrlSync`) doesn't break hits.
- `VideoCacheManager.removeVideoCache` mirrors the same path-based key.
- Previously `[Música: ...]` music was a mock, now real audioUrl support.
